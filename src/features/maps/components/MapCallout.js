import React from "react";
import SharedRestaurantInfo from "../../../components/restaurants/SharedRestaurantInfo";

const MapCallout = ({ restaurant }) => {
  return <SharedRestaurantInfo restaurant={restaurant} />;
};

export default MapCallout;
